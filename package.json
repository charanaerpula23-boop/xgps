{
  "name": "location-server",
  "version": "1.0.0",
  "description": "Simple Express server to receive GPS coordinates from Android app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
